{
  "_comment": "Production: least logging, and from Phase 6 the metrics and diagnostics endpoints sit behind auth.",

  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "ThePlayer": "Information"
    }
  }
}
